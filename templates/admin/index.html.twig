{% extends 'base.html.twig' %}

{% block title %}Home - Admin{% endblock %}

{% block body %}
    <p class="fs-1 text-white text-center">Admin page</p>
    <div class="d-flex gap-2">
        <a href="{{ path('admin_users') }}"><button class="btn btn-primary">User Manager</button></a>
        <a href="{{ path('admin_training') }}"><button class="btn btn-primary">Trainings Manager</button></a>
        <a href="{{ path('admin_lesson') }}"><button class="btn btn-primary">Lessons Manager</button></a>
    </div>
{#    <div class="d-flex flex-column gap-2">#}
{#        {% for user in users %}#}
{#            <div class="d-flex p-2 gap-2 rounded-2 align-items-center" style="background: rgba(255,255,255,0.05)">#}
{#                <div class="d-flex flex-shrink-0 justify-content-center align-items-center rounded-circle" style="background: rgba(0,0,0,0.2); width: 2rem; height: 2rem; font-size: 0.8rem">#}
{#                    {{ user.id }}#}
{#                </div>#}
{#                <div>#}
{#                    {{ user.username }}#}
{#                </div>#}
{#                <div style="font-size: 0.8rem; color: rgba(255,255,255,0.4);">#}
{#                    {{ user.roles|join(', ') }}#}
{#                </div>#}
{#                <div class="flex-grow-1"></div>#}
{#                <form method="post">#}
{#                    {% if not user.hasRole('ROLE_INSTRUCTOR') %}#}
{#                        <button formaction="{{ path('add_instructor', {'user': user.id}) }}" class="btn btn-sm btn-success">#}
{#                            Make Instructor#}
{#                        </button>#}
{#                    {% endif %}#}
{#                    {% if user.hasRole('ROLE_INSTRUCTOR') %}#}
{#                        <button formaction="{{ path('remove_instructor', {'user': user.id}) }}" class="btn btn-sm btn-danger">#}
{#                            Remove Instructor#}
{#                        </button>#}
{#                    {% endif %}#}
{#                </form>#}
{#            </div>#}
{#        {% endfor %}#}

{#        {% for instructor in instructors %}#}
{#            <div>{{ instructor.username }}</div>#}
{#        {% endfor %}#}
{#    </div>#}
{% endblock %}
